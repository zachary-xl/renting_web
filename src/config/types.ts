export interface IConfig {
  appEnv: string;
  projectName: string;
  projectDesc: string;
  base: string;
  baseUrl: string;
  serverUrl: string;
  timeout: number;
  storage: string;
  storageIsExpire: boolean;
}
